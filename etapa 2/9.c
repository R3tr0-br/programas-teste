/*
verifique se um aluno foi reprovado:
média >=7.0 -> aprovado
média >= -> recuperação
senão -> reprovado
usando loop
*/
#include <stdio.h>
int main(void){
    int num;
    printf("Digite as notas (digite 0 para parar): ");
    scanf("%d", num);

    while (num != 0){
        
    }
}